#include "main.h"

/**
 * _puts - prints a string, followed by aa new line.
 * @str: printer to the string to print
 * Return: void
 */

void _puts(char *str)
{
	int = 0;

	while (str[i])
	{
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}
